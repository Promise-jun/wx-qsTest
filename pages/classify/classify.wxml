<view class='classify-box' bindtap='openTestList'>
  <block wx:for="{{classifyList}}" wx:key="{{item.id}}">
    <view class='classify-item'>
      <image src="{{item.picUrl}}"></image>
      <text>{{item.title}}</text>
    </view>
  </block>
</view>
